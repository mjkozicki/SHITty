version: '3.8'

services:
  shitty-api:
    build: .
    ports:
      - "1009:1009"
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:1009/api/v1/products"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
