version: '3.8'

services:
  shitty-api:
    build: .
    ports:
      - "3001:3001"
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/v1/products"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
